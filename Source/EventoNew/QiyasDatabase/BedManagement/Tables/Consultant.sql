CREATE TABLE [BedManagement].[Consultant] (
    [ConsultantId]   INT           NOT NULL,
    [ConsultantCode] NVARCHAR (50) NULL,
    CONSTRAINT [PK_Consultant] PRIMARY KEY CLUSTERED ([ConsultantId] ASC)
);

